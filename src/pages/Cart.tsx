import NavBar from '../componants/NavBar'

export default function CartPage() {
  return (
    <main>
      <NavBar />
      <section>
        <h1>Cart</h1>
        <p>Cart placeholder.</p>
      </section>
    </main>
  )
}